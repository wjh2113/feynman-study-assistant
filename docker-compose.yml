# 本地/京东云主机同 VPC 基础设施。
# 生产文件请用京东云 OSS（STORAGE_PROVIDER=jdcloud），不要把上传目录挂在本 compose 里。
# 详见 docs/jdcloud-storage.md
services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: zhifan-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: zhifan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - zhifan_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d zhifan"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.4-alpine
    container_name: zhifan-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - zhifan_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  zhifan_postgres_data:
  zhifan_redis_data:
